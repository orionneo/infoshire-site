/* stylelint-disable */
@tailwind base;
@tailwind components;
@tailwind utilities;

/* Definition of the design system. All colors, gradients, fonts, etc should be defined here.
All colors MUST be HSL.
*/

@layer base {
  :root {
    --radius: 0.5rem;
    
    /* InfoShire - Force Dark Mode with Black and Neon Green */
    --background: 0 0% 0%;
    --foreground: 0 0% 98%;
    
    --card: 0 0% 8%;
    --card-foreground: 0 0% 98%;
    
    --popover: 0 0% 8%;
    --popover-foreground: 0 0% 98%;
    
    /* Primary: Neon Green */
    --primary: 120 100% 50%;
    --primary-foreground: 0 0% 0%;
    
    /* Secondary: Dark gray */
    --secondary: 0 0% 15%;
    --secondary-foreground: 0 0% 98%;
    
    /* Muted: Dark neutral */
    --muted: 0 0% 15%;
    --muted-foreground: 0 0% 70%;
    
    /* Accent: Neon green accent */
    --accent: 0 0% 15%;
    --accent-foreground: 120 100% 50%;
    
    --destructive: 0 63% 31%;
    --destructive-foreground: 0 0% 98%;
    
    --border: 0 0% 20%;
    --input: 0 0% 20%;
    --ring: 120 100% 50%;
    
    /* Success, warning, info states */
    --success: 120 100% 50%;
    --warning: 38 92% 50%;
    --info: 199 89% 48%;
    
    /* Chart colors - vibrant neon */
    --chart-1: 120 100% 50%;
    --chart-2: 180 100% 50%;
    --chart-3: 38 92% 60%;
    --chart-4: 271 81% 65%;
    --chart-5: 339 90% 60%;
    
    /* Sidebar - dark with neon accent */
    --sidebar-background: 0 0% 0%;
    --sidebar-foreground: 0 0% 98%;
    --sidebar-primary: 120 100% 50%;
    --sidebar-primary-foreground: 0 0% 0%;
    --sidebar-accent: 0 0% 15%;
    --sidebar-accent-foreground: 120 100% 50%;
    --sidebar-border: 0 0% 20%;
    --sidebar-ring: 120 100% 50%;
  }

  .dark {
    --background: 0 0% 0%;
    --foreground: 0 0% 98%;
    
    --card: 0 0% 8%;
    --card-foreground: 0 0% 98%;
    
    --popover: 0 0% 8%;
    --popover-foreground: 0 0% 98%;
    
    /* Primary: Bright neon green for dark mode */
    --primary: 120 100% 50%;
    --primary-foreground: 0 0% 0%;
    
    /* Secondary: Dark gray */
    --secondary: 0 0% 15%;
    --secondary-foreground: 0 0% 98%;
    
    /* Muted: Dark neutral */
    --muted: 0 0% 15%;
    --muted-foreground: 0 0% 70%;
    
    /* Accent: Neon green accent */
    --accent: 0 0% 15%;
    --accent-foreground: 120 100% 50%;
    
    --destructive: 0 63% 31%;
    --destructive-foreground: 0 0% 98%;
    
    --border: 0 0% 20%;
    --input: 0 0% 20%;
    --ring: 120 100% 50%;
    
    /* Success, warning, info states */
    --success: 120 100% 50%;
    --warning: 38 92% 50%;
    --info: 199 89% 48%;
    
    /* Chart colors - vibrant neon for dark mode */
    --chart-1: 120 100% 50%;
    --chart-2: 180 100% 50%;
    --chart-3: 38 92% 60%;
    --chart-4: 271 81% 65%;
    --chart-5: 339 90% 60%;
    
    /* Sidebar - dark with neon accent */
    --sidebar-background: 0 0% 0%;
    --sidebar-foreground: 0 0% 98%;
    --sidebar-primary: 120 100% 50%;
    --sidebar-primary-foreground: 0 0% 0%;
    --sidebar-accent: 0 0% 15%;
    --sidebar-accent-foreground: 120 100% 50%;
    --sidebar-border: 0 0% 20%;
    --sidebar-ring: 120 100% 50%;
  }
}

@layer base {
  * {
    @apply border-border;
  }
  
  body {
    @apply bg-background text-foreground;
    font-feature-settings: "rlig" 1, "calt" 1;
  }
  
  /* Force dark mode */
  html {
    color-scheme: dark;
  }
}

@layer utilities {
  .gradient-text {
    background: linear-gradient(135deg, hsl(var(--primary)), hsl(120 100% 40%));
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    color: transparent;
  }
  
  .neon-glow {
    text-shadow: 0 0 10px hsl(var(--primary)), 0 0 20px hsl(var(--primary)), 0 0 30px hsl(var(--primary));
  }
  
  .neon-border {
    border: 1px solid hsl(var(--primary));
    box-shadow: 0 0 10px hsl(var(--primary) / 0.5);
  }
  
  .neon-hover {
    transition: all 0.3s ease;
  }
  
  .neon-hover:hover {
    border-color: hsl(var(--primary));
    box-shadow: 0 0 10px hsl(var(--primary) / 0.5);
  }
  
  @keyframes float {
    0%, 100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-20px);
    }
  }
  
  .animate-float {
    animation: float 3s ease-in-out infinite;
  }
  
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  
  .animate-fade-in-up {
    animation: fadeInUp 0.8s ease-out forwards;
  }
  
  @keyframes slideInLeft {
    from {
      opacity: 0;
      transform: translateX(-30px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }
  
  .animate-slide-in-left {
    animation: slideInLeft 0.8s ease-out forwards;
  }
  
  @keyframes pulse-glow {
    0%, 100% {
      box-shadow: 0 0 20px hsl(var(--primary) / 0.3);
    }
    50% {
      box-shadow: 0 0 40px hsl(var(--primary) / 0.6);
    }
  }
  
  .animate-pulse-glow {
    animation: pulse-glow 2s ease-in-out infinite;
  }
  
  .hover-lift {
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }
  
  .hover-lift:hover {
    transform: translateY(-8px);
    box-shadow: 0 10px 30px hsl(var(--primary) / 0.3);
  }
  
  /* Gamer aesthetic utilities */
  .gamer-grid {
    background-image: 
      linear-gradient(hsl(var(--primary) / 0.05) 1px, transparent 1px),
      linear-gradient(90deg, hsl(var(--primary) / 0.05) 1px, transparent 1px);
    background-size: 50px 50px;
  }
  
  .circuit-pattern {
    background-image: 
      radial-gradient(circle at 20% 50%, hsl(var(--primary) / 0.1) 0%, transparent 50%),
      radial-gradient(circle at 80% 80%, hsl(var(--primary) / 0.08) 0%, transparent 50%);
  }
  
  /* Premium Button Utilities */
  .btn-premium {
    position: relative;
    overflow: hidden;
    border: 1px solid hsl(var(--primary) / 0.6);
    box-shadow: 0 0 15px hsl(var(--primary) / 0.3), inset 0 0 10px hsl(var(--primary) / 0.1);
    transition: all 0.3s ease;
  }
  
  .btn-premium::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, hsl(var(--primary) / 0.4), transparent);
    transition: left 0.5s ease;
  }
  
  .btn-premium:hover {
    transform: translateY(-1px);
    box-shadow: 0 0 25px hsl(var(--primary) / 0.5), 0 4px 12px rgba(0, 0, 0, 0.3), inset 0 0 15px hsl(var(--primary) / 0.15);
    border-color: hsl(var(--primary));
  }
  
  .btn-premium:hover::before {
    left: 100%;
  }
  
  .btn-premium:focus-visible {
    outline: none;
    ring: 2px;
    ring-color: hsl(var(--primary));
    ring-offset: 2px;
  }
  
  .btn-premium-outline {
    position: relative;
    overflow: hidden;
    background: hsl(var(--card) / 0.4);
    backdrop-filter: blur(8px);
    border: 1px solid hsl(var(--primary) / 0.35);
    box-shadow: 0 0 10px hsl(var(--primary) / 0.2);
    transition: all 0.3s ease;
  }
  
  .btn-premium-outline::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, hsl(var(--primary) / 0.2), transparent);
    transition: left 0.5s ease;
  }
  
  .btn-premium-outline:hover {
    transform: translateY(-1px);
    background: hsl(var(--primary) / 0.1);
    border-color: hsl(var(--primary));
    box-shadow: 0 0 20px hsl(var(--primary) / 0.4), 0 4px 12px rgba(0, 0, 0, 0.3);
  }
  
  .btn-premium-outline:hover::before {
    left: 100%;
  }
  
  .btn-premium-outline:focus-visible {
    outline: none;
    ring: 2px;
    ring-color: hsl(var(--primary));
    ring-offset: 2px;
  }
}

@layer base {
  * {
    @apply border-border;
  }

  body {
    @apply bg-background text-foreground;
  }
}

@layer utilities {
  .scrollbar-hide {
    -ms-overflow-style: none;
    scrollbar-width: none;
  }
  
  .scrollbar-hide::-webkit-scrollbar {
    display: none;
  }
}
