/**
 * ✅ processOfflineQueue (disabled)
 * Admin is back to the "direct online" behavior.
 * If you want offline-first again later, we can reintroduce it carefully.
 */
export async function processOfflineQueue(): Promise<void> {
  return;
}
